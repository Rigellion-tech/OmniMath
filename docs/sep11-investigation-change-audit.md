# Sep 11 investigation — final change/provenance audit

## Baseline and attribution

**Git HEAD was NOT the Sep 11 investigation baseline.** The tree already contained typed/OCR unification, mathematical trust-boundary/calibration work, deep-explanation provenance, Home operation/revision handling, semantic grammar/rendering work, and widespread line-ending differences. Attribution uses the retained investigation record and milestone documents, not the entire Git diff. Do not revert whole mixed files.

All four observed presentation classes remain **NOT CONCLUSIVELY ATTRIBUTABLE**. The inventory below distinguishes demonstrated architecture from historical incident attribution. See [the final reconstruction](sep11-presentation-incident-reconstruction.md) for evidence and exact command results; [the handoff](omnimath-next-session-handoff.md) is the fresh-session entry point.

## Retained investigation-created production hunks

| File / function | Exact retained behavior | Demonstrated reason / incident relevance | Regression crossing the boundary |
| --- | --- | --- | --- |
| server/app.js / copyInternalRequest, internal image adapters | Explicitly copy real request headers into internal extraction/solve requests | IncomingMessage spread loses authorization; real defect, consistent with S1 auth logs, **not attributed to either visibly failed solve** | incidentBackendBoundaries: real Node request + actual Clerk verification in production mode + route/parser/saved identity; unauthorized call401 without provider. internalRequestAuth is supplementary adapter coverage, not sole proof |
| server/app.js / handleExplainRequest, buildResponse, save helpers | Observe history save without awaiting it; report pending/immediate auth warning, maintain warning handling | Real unresolved pg promise blocked successful HTTP; DB rejection alone did not prove this. Potential apparent solve failure, not proven Sep11 | incidentBackendBoundaries: HTTP200 before deferred pg query resolves, then ECONNRESET; saveAuthExpiry reused |
| server/app.js / solve/extraction diagnostics | Carry requestId into response; AsyncLocalStorage around solve; correlate OCR imageHash/review, policy rejection, boundary diagnostics and save warnings; post-annotation structural check | Old trace lacked linkage across provider/candidate/render/save; diagnostics only, no routing redesign | incidentBackendBoundaries; incidentBlankBoundaries; browser request/step attributes |
| server/solveDiagnosticContext.js | Request-local diagnostic context only | Concurrent backend boundary logs need correct request correlation, not global mutable request state | incidentBlankBoundaries and incidentBackendBoundaries |
| server/ocrSolvePolicy.js; app extracted-solve policy call | Read canonical extractionValidation.issues/critical; reject silent direct with existing structural finding/critical; allow explicit edited/anyway | Old server trusted default/direct caller despite canonical review finding. **No reasons/issues shape bug** and no O1/solve correlation | ocrSolvePolicy; incidentBackendBoundaries crosses client submission → JSON → endpoint with conflicting outer/canonical review |
| server/solveCandidateStructure.js | Extend existing structural inspector for renderability, original source/destination fields/indexes, explicit empty classifications and correlated error diagnostics | Layout-only/invalid math and source-to-destination loss must not silently become normal steps. Not a mathematical correctness validator | incidentBlankBoundaries, solveCandidateLifecycle, fastSolvePipeline |
| server/mathExplanationSchema.js | Structural checks before empty-step filtering, pre-sanitization provider summaries, source/destination checks in fast/compact/image conversion | Legacy image step removal and missing normalization evidence demonstrated; **S1 uses shared fast conversion, not this legacy image path** | fastSolvePipeline actual image/fast converters; incidentBlankBoundaries actual empty/whitespace converter rejection |
| server/solveCandidateLifecycle.js; app local-rule finalization | Retain structural check/context and allow pre-existing local teaching cards to omit provider-style final LaTeX | Compatibility with stricter investigation guard, not a presentation cause; verifier logic unchanged | Existing candidate/local-rule/usage/save tests |
| src/lib/solutionSteps.js / getRenderableSolutionSteps | Live response selects a complete nested candidate; otherwise preserves original positions with explicit boundary-error steps | [{}] masking and silent malformed-middle filtering demonstrated. Legacy status/merge lookup behavior restored; dirty/protected merge logic pre-existing | solveResponseNormalization; incidentBlankBoundaries; actual empty card browser fixture |
| src/api/mathClient.js / normalizeSolveResponse | Log raw candidate summaries before selection; retain requestId/save status; emit accepted_empty at client selection | Previously diagnostics described transformed output as received/provider data; missing client/request linkage | incidentBlankBoundaries; solveResponseNormalization; backend/browser request correlation |
| src/components/math/MathStep.jsx | Trim whitespace lines/chunks before fallback; zero-block split reaches real render-input boundary; explicit error body stays visible independent of reasoning expansion; step/request/index attributes | Whitespace display arrays suppressed valid math; split bypassed renderer; first placeholder could be hidden in reasoning. Actual component defects/diagnostic gaps, no Sep11 payload | layoutRegression malformed math and blank render output cases |
| src/components/math/MathRenderer.jsx | Visible safe error text; distinguish input-empty, render-failed, rendered-empty and hidden/unmeasurable DOM; preserve visible smash/rule content | Old catch cleared host; first investigation placeholder was hidden by legacy data-math-fallback CSS. Safe placeholder uses distinct attribute. DOM measurement must not erase valid hidden-container math | layoutRegression real KaTeX/DOM cases; semantic/browser suites. No CSS style redesign |
| src/components/math/SolutionFlow.jsx; ProblemBlock.jsx | Pass diagnostic requestId to flow/steps; attributes only. Original step.id key retained | Connect returned solve to actual rendered step | layoutRegression requestId/index assertions |
| src/components/math/ExplanationPanel.jsx / lazy transport, hook and tooltip | Add transport/owner IDs and truthful parse/cache/failure/owner/post-commit/body-DOM observations, including empty body/input and pre-observation owner disappearance; original cancellation/timing retained | Successful provider rows had no downstream attribution; earlier lease theory disproven. Instrumentation, not a claimed Sep11 hover fix | hoverLifecycle actual component success/hidden/stale/parsed-whitespace cases; hoverRequestLifecycle only classifies error vocabulary |
| ExplanationPanel.jsx / FollowupChat; src/lib/followupLifecycle.js | Add accurate request/owner/API/post-commit/assistant-DOM/local-window events and assistant requestId metadata | Missing browser evidence near U1/U2. Core reducer/provenance/persistence independence pre-existing; no causal persistence patch | incidentFollowup actual Home POST/PUT/list/stale-save cases; existing followup lifecycle/ownership tests |

No new environment variables, solver routes, mathematical verification policy, problem-specific rules, or auth bypass were introduced. Development fallback remains explicitly local; the corrected authenticated production route is tested not to depend on it.

## Investigation-created changes reverted or superseded

| Removed change | Why it was unsupported or incorrect |
| --- | --- |
| Shared-request lease/controller/deadline-extension refactor; src/lib/sharedRequestRegistry.js and tests/sharedRequestRegistry.test.mjs | Based on false claim that tooltip leave/unmount aborted transport. Old cleanup did not abort; helper tests proved only the new abstraction. Files and imports removed |
| Independent OCR high-tier requirement, difference threshold .62 and substantial/cleanup-ratio .35 gate | Not justified by canonical structural review contract or correlated incident. Canonical critical/issue flags retained; raw numbers diagnostic only |
| Candidate duplicate-ID rejection, client duplicate suffixes, position-appended React keys | No demonstrated blank-position mechanism. Original IDs/keys preserved; no claim duplicate keys caused Sep11 |
| Filtering malformed positions in legacy getSolutionSteps/withNormalizedSolutionSteps | Changed existing status/history behavior without proving a failure; restored legacy lookup. Live API normalization retains explicit error positions instead |
| Incorrect classification of visible \smash as invisible | Layout-only heuristic was too broad; visible glyph remains accepted and browser-tested |
| Normalization-empty inference from re-normalizing a source instead of comparing source and destination | Diagnostic oracle was wrong and missed early-empty returns; actual source/destination check now retains field/index |
| Pre-filtered summaries labeled api-parsed/provider data | Misstated observation stage; raw client candidate summaries precede selection, provider summaries precede sanitization |
| Follow-up commit event emitted before dispatch; persistence_requested/unused persistence_failed names | Earlier stages were mislabeled. Connected assistant commit/DOM observation and local_window_update_requested reflect actual stages |
| Generic browser failures labeled provider_failed; parsed-empty follow-up labeled parse_failed | Browser sees API/network/shape failures, not necessarily provider failures. HTTP status alone is not provider evidence |
| Helper-only manually emitted hover success/render test; reducer plus unrelated rejected-promise persistence test | Did not cross the production lifecycle responsible for the claimed invariant; replaced by real component/Home tests |
| Exact-one transport/rehover test hypothesis and narrow timing assumptions | Not a demonstrated old failure; settings/effect restarts can legitimately add owners/requests. Hidden-owner/transport classification is tested directly |
| Error placeholder carrying data-math-fallback=true | Existing CSS deliberately hides raw fallback; it also hid the new safe text. Real browser regression exposed the incomplete fix; safe placeholder now distinct |
| Transparent-color fixture expected to be invisible | Existing math CSS forces inherited color; fixture oracle was wrong. Spacing-only output tests genuinely empty glyph DOM |
| Earlier report statements that hover lease, duplicate keys or OCR64 caused presentation failures | Unsupported causation removed; S1 fast-path and json_parse log semantics corrected |

### Changes made before sufficient proof

The initial lease refactor, duplicate-ID changes and broad OCR thresholds were speculative and are removed. The history-background change preceded a sufficiently realistic stalled-save regression; it is retained only after the real route/deferred-pg test demonstrated the violated invariant. Initial structural/render diagnostics were incomplete (smash, source comparison, CSS-hidden error, zero-block split, collapsed message); these were exposed and corrected through boundary/browser tests, not rationalized as Sep11 causes.

## Regression inventory and strength

- New investigation tests: incidentBackendBoundaries, incidentBlankBoundaries, internalRequestAuth, ocrSolvePolicy, hoverRequestLifecycle, hoverLifecycle.layoutRegression, incidentFollowup.layoutRegression.
- Existing/mixed tests extended: fastSolvePipeline, solveCandidateLifecycle, solveResponseNormalization, layoutRegression, followupLifecycle.
- incidentBlankBoundaries explicitly labels visible-source-to-empty and post-annotation corruption as **fault injection at a production assertion boundary**. It does not claim the current normalizer erases that valid expression. Actual converter and DOM tests cover separately demonstrated paths.
- Recorder/error-classifier unit tests remain useful unit tests, but are not counted as API/UI integration proof.
- Existing skipped math-pipeline corpus cases were preserved, not enabled/disabled to make this incident pass.
- saveAuthExpiry has no semantic HEAD difference when line endings are ignored: **reused, not investigation-created**.
- Canonical solve routing tests, verifier/calibration suites, follow-up provenance routes, core reducer/retry tests, sessionIsolation and semantic-hardening suites are pre-existing work, even where files are untracked.

## Other dirty work that must be preserved

server/app.js, server/mathExplanationSchema.js, src/api/mathClient.js, ExplanationPanel.jsx and src/lib/solutionSteps.js have especially mixed provenance. server/solveCandidateStructure.js and solveCandidateLifecycle.js themselves were created by earlier trust-boundary work; only the incident hunks above belong here. server/solveAcceptancePolicy.js, server/verification, src/lib/explanationProvenance.js, api/explain-followup.js provenance work, Home operation/revision handling, HoverContext ownership, semantic parser/renderer/hitboxes and related tests are not incident-created architecture.

The exact final command ledger is in reconstruction section G: focused111 tests (94 pass/17 skip), full offline1110 (1089 pass/21 skip), final serial browser50/50; zero final failures/todo. Lint/typecheck/build pass. An earlier parallel browser49 run had48 pass/1 semantic-selection failure; it remains recorded, not retroactively declared fixed. No commit was made. No unrelated dirty files were restored or discarded. Production editing stopped before final verification/documentation closure.
